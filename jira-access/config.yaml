# Jira 账号邮箱。优先使用环境变量 JIRA_USERNAME。
username: "your-email@example.com"

# Jira API token 或 PAT。优先使用环境变量 JIRA_API_TOKEN。
api-token: "your-api-token"

# Jira 基础地址。优先使用环境变量 JIRA_BASE_URL。
base_url: "https://your-domain.atlassian.net"
